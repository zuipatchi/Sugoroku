namespace Common.Character
{
    /// <summary>
    /// 選択可能なキャラクターの識別子。表示名・イラストの Addressable アドレスは
    /// <see cref="CharacterCatalog"/> が対応付ける。種類を増やすときはここに追加する。
    /// </summary>
    public enum CharacterId
    {
        Character1,
        Character2,
        Character3,
        Character4,
    }
}
