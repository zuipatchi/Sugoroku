namespace Common.GameSession
{
    /// <summary>
    /// ゲームのプレイ形態。Home で選択し Main の挙動（NGO 起動の有無など）を切り替える。
    /// </summary>
    public enum GameMode
    {
        Online,
        SinglePlayer,
    }
}
