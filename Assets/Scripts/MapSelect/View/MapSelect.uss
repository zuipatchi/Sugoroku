.ms-root {
    flex-grow: 1;
    background-color: rgb(22, 22, 35);
    align-items: center;
    justify-content: space-between;
}

/* 全画面背景（Image/StageBackground）--------------------------------------------- */

/* 全画面枠。はみ出しをここで隠す。入力は奪わない。 */
.ms-bg {
    position: absolute;
    left: 0;
    top: 0;
    right: 0;
    bottom: 0;
    overflow: hidden;
}

/* ステージ選択の背景画像。全画面をカバーするよう scale-and-crop で敷く。 */
.ms-bg__image {
    position: absolute;
    left: 0;
    top: 0;
    right: 0;
    bottom: 0;
    -unity-background-scale-mode: scale-and-crop;
}

/* 前面 UI を読みやすくする暗いスクリム（幕）。 */
.ms-bg__scrim {
    position: absolute;
    left: 0;
    top: 0;
    right: 0;
    bottom: 0;
    background-color: rgba(15, 15, 26, 0.55);
}

/* 操作エリア。カード一覧の下＝画面下部の暗い帯の中に中央寄せで置く（CharacterSelect と同じ）。 */
.ms-actions {
    flex-direction: row;
    justify-content: center;
    margin-top: 16px;
}

.ms-action {
    width: 200px;
    margin-left: 10px;
    margin-right: 10px;
}

/* 選択中マップの大プレビュー領域。中央に大きく盤面サムネイルとマップ名を出す。 */
.ms-preview-wrap {
    flex-grow: 1;
    width: 100%;
    align-items: center;
    justify-content: center;
}

/* ここの寸法は「基準ボックス」で、実際の枠は MapPickerView が選択マップの縦横比を
   この中へ内接させた大きさに変える（横長マップなら横長の枠になる）。 */
.ms-preview {
    width: 320px;
    height: 320px;
    max-width: 80%;
    /* 背景画像の上でも盤面サムネイルが読めるよう、暗い地色を敷く。 */
    background-color: rgba(22, 22, 35, 0.75);
    border-left-width: 2px;
    border-right-width: 2px;
    border-top-width: 2px;
    border-bottom-width: 2px;
    border-left-color: rgba(255, 255, 255, 0.12);
    border-right-color: rgba(255, 255, 255, 0.12);
    border-top-color: rgba(255, 255, 255, 0.12);
    border-bottom-color: rgba(255, 255, 255, 0.12);
    border-top-left-radius: 16px;
    border-top-right-radius: 16px;
    border-bottom-left-radius: 16px;
    border-bottom-right-radius: 16px;
}

.ms-title {
    margin-top: 16px;
    font-size: 24px;
    color: rgb(240, 240, 255);
    -unity-text-align: middle-center;
}

/* 選択中マップの総マス数（内訳の見出し）。 */
.ms-cellcount {
    margin-top: 6px;
    font-size: 15px;
    color: rgba(230, 230, 245, 0.75);
    -unity-text-align: middle-center;
}

/* イベント別の内訳チップを横並び・折り返しで中央に置く。 */
.ms-stats {
    flex-direction: row;
    flex-wrap: wrap;
    justify-content: center;
    align-items: center;
    margin-top: 8px;
    max-width: 90%;
}

/* 1 イベント分のチップ（色ドット＋「ラベル ×N」）。 */
.ms-stat {
    flex-direction: row;
    align-items: center;
    margin-left: 5px;
    margin-right: 5px;
    margin-top: 4px;
    margin-bottom: 4px;
    padding-top: 3px;
    padding-bottom: 3px;
    padding-left: 8px;
    padding-right: 10px;
    background-color: rgba(22, 22, 35, 0.85);
    border-left-width: 1px;
    border-right-width: 1px;
    border-top-width: 1px;
    border-bottom-width: 1px;
    border-left-color: rgba(255, 255, 255, 0.14);
    border-right-color: rgba(255, 255, 255, 0.14);
    border-top-color: rgba(255, 255, 255, 0.14);
    border-bottom-color: rgba(255, 255, 255, 0.14);
    border-top-left-radius: 12px;
    border-top-right-radius: 12px;
    border-bottom-left-radius: 12px;
    border-bottom-right-radius: 12px;
}

/* チップ左端の色ドット（サムネイルの塗り色と対応）。実際の色はコードで設定する。 */
.ms-stat__dot {
    width: 12px;
    height: 12px;
    margin-right: 6px;
    border-top-left-radius: 6px;
    border-top-right-radius: 6px;
    border-bottom-left-radius: 6px;
    border-bottom-right-radius: 6px;
    border-left-width: 1px;
    border-right-width: 1px;
    border-top-width: 1px;
    border-bottom-width: 1px;
    border-left-color: rgba(0, 0, 0, 0.5);
    border-right-color: rgba(0, 0, 0, 0.5);
    border-top-color: rgba(0, 0, 0, 0.5);
    border-bottom-color: rgba(0, 0, 0, 0.5);
}

.ms-stat__label {
    font-size: 14px;
    color: rgb(235, 235, 248);
    -unity-text-align: middle-center;
}

/* 画面下部のカード一覧エリア。背景画像の上でも視認できるよう暗い帯を敷く。 */
.ms-bottom {
    width: 100%;
    align-items: center;
    padding-top: 16px;
    padding-bottom: 20px;
    background-color: rgba(0, 0, 0, 0.5);
}

/* プレイヤー人数（自分＋CPU）のステッパー行。ラベル＋−／数値／＋。カード一覧の上に置く。 */
.ms-players {
    flex-direction: row;
    align-items: center;
    justify-content: center;
    margin-bottom: 14px;
}

.ms-players__label {
    margin-right: 14px;
    font-size: 18px;
    color: rgb(240, 240, 255);
    -unity-text-align: middle-center;
}

/* −／数値／＋ をまとめる暗いピル。 */
.ms-stepper {
    flex-direction: row;
    align-items: center;
    background-color: rgba(22, 22, 35, 0.85);
    border-left-width: 1px;
    border-right-width: 1px;
    border-top-width: 1px;
    border-bottom-width: 1px;
    border-left-color: rgba(255, 255, 255, 0.2);
    border-right-color: rgba(255, 255, 255, 0.2);
    border-top-color: rgba(255, 255, 255, 0.2);
    border-bottom-color: rgba(255, 255, 255, 0.2);
    border-top-left-radius: 10px;
    border-top-right-radius: 10px;
    border-bottom-left-radius: 10px;
    border-bottom-right-radius: 10px;
    padding-top: 4px;
    padding-right: 6px;
    padding-bottom: 4px;
    padding-left: 6px;
}

/* −／＋ の丸ボタン。金アクセントの文字に暗い座面。 */
.ms-stepper__btn {
    width: 40px;
    height: 40px;
    padding-top: 0;
    padding-right: 0;
    padding-bottom: 0;
    padding-left: 0;
    margin-left: 4px;
    margin-right: 4px;
    font-size: 24px;
    color: rgb(24, 24, 36);
    background-color: rgb(235, 200, 90);
    border-left-width: 0;
    border-right-width: 0;
    border-top-width: 0;
    border-bottom-width: 0;
    border-top-left-radius: 20px;
    border-top-right-radius: 20px;
    border-bottom-left-radius: 20px;
    border-bottom-right-radius: 20px;
    -unity-text-align: middle-center;
}

.ms-stepper__btn:hover {
    background-color: rgb(255, 225, 120);
}

.ms-stepper__btn:active {
    scale: 0.94 0.94;
}

.ms-stepper__btn:disabled {
    opacity: 0.3;
}

/* 現在の人数。 */
.ms-stepper__value {
    min-width: 44px;
    font-size: 26px;
    color: rgb(255, 226, 140);
    -unity-text-align: middle-center;
}

/* カードを横並び・折り返しで中央に配置する。 */
.ms-grid {
    flex-direction: row;
    flex-wrap: wrap;
    justify-content: center;
    align-items: center;
    max-width: 90%;
}

/* マップ 1 枚分のカード（Button）。サムネイル＋名前。 */
.map-card {
    width: 110px;
    height: 138px;
    margin-left: 8px;
    margin-right: 8px;
    margin-top: 6px;
    margin-bottom: 6px;
    padding-top: 8px;
    padding-right: 8px;
    padding-bottom: 8px;
    padding-left: 8px;
    background-color: rgba(22, 22, 35, 0.85);
    border-left-width: 2px;
    border-right-width: 2px;
    border-top-width: 2px;
    border-bottom-width: 2px;
    border-left-color: rgba(255, 255, 255, 0.2);
    border-right-color: rgba(255, 255, 255, 0.2);
    border-top-color: rgba(255, 255, 255, 0.2);
    border-bottom-color: rgba(255, 255, 255, 0.2);
    border-top-left-radius: 12px;
    border-top-right-radius: 12px;
    border-bottom-left-radius: 12px;
    border-bottom-right-radius: 12px;
    align-items: center;
    justify-content: flex-start;
    transition-property: scale, border-color;
    transition-duration: 0.1s, 0.1s;
}

.map-card:hover {
    scale: 1.04 1.04;
}

.map-card:active {
    scale: 0.97 0.97;
}

/* 選択中のカードはゴールドの枠で強調する。 */
.map-card--selected {
    border-left-color: rgb(235, 200, 90);
    border-right-color: rgb(235, 200, 90);
    border-top-color: rgb(235, 200, 90);
    border-bottom-color: rgb(235, 200, 90);
    scale: 1.04 1.04;
}

/* カードの盤面サムネイル領域（正方形）。中身は Painter2D でコード描画する。 */
.map-thumb {
    width: 90px;
    height: 90px;
    border-top-left-radius: 8px;
    border-top-right-radius: 8px;
    border-bottom-left-radius: 8px;
    border-bottom-right-radius: 8px;
    background-color: rgba(255, 255, 255, 0.06);
}

.map-name {
    margin-top: 8px;
    font-size: 14px;
    color: rgb(240, 240, 255);
    -unity-text-align: middle-center;
}

/* アクセントボタン（design-system.md 準拠） */
.btn-accent {
    background-color: rgb(70, 90, 180);
    color: rgb(255, 255, 255);
    border-top-left-radius: 8px;
    border-top-right-radius: 8px;
    border-bottom-left-radius: 8px;
    border-bottom-right-radius: 8px;
    border-left-width: 0;
    border-right-width: 0;
    border-top-width: 0;
    border-bottom-width: 0;
    padding-top: 14px;
    padding-right: 10px;
    padding-bottom: 14px;
    padding-left: 10px;
    font-size: 22px;
    -unity-text-align: middle-center;
}

.btn-accent:hover {
    background-color: rgb(85, 105, 198);
}

.btn-accent:focus {
    background-color: rgb(95, 115, 210);
}

.btn-accent:active {
    background-color: rgb(50, 65, 145);
    scale: 0.96 0.96;
}

/* セカンダリボタン（design-system.md 準拠） */
.btn-secondary {
    background-color: rgba(22, 22, 35, 0.85);
    color: rgb(230, 230, 245);
    border-top-left-radius: 8px;
    border-top-right-radius: 8px;
    border-bottom-left-radius: 8px;
    border-bottom-right-radius: 8px;
    border-left-width: 1px;
    border-right-width: 1px;
    border-top-width: 1px;
    border-bottom-width: 1px;
    border-left-color: rgba(255, 255, 255, 0.15);
    border-right-color: rgba(255, 255, 255, 0.15);
    border-top-color: rgba(255, 255, 255, 0.15);
    border-bottom-color: rgba(255, 255, 255, 0.15);
    padding-top: 14px;
    padding-right: 10px;
    padding-bottom: 14px;
    padding-left: 10px;
    font-size: 22px;
    -unity-text-align: middle-center;
}

.btn-secondary:hover {
    background-color: rgba(45, 45, 65, 0.9);
}

.btn-secondary:focus {
    background-color: rgba(55, 55, 78, 0.92);
}

.btn-secondary:active {
    background-color: rgba(15, 15, 25, 0.9);
    scale: 0.96 0.96;
}
