# CLAUDE.md

このファイルはリポジトリで作業する Claude Code (claude.ai/code) へのガイダンスを提供します。

## プロジェクト概要

オンライン対戦すごろく。**縦画面（ポートレート）**。Unity 6 (6000.3.18f1) 製。BGM/SE 再生機能と、Commonシーンをベースとしたアディティブシーン管理、UGS Multiplayer Services によるオンラインマッチングを備える。Home で「一人用モード」「オンラインプレイ」を選択でき、一人用モードはネットワーク非依存で、キャラクター選択（CharacterSelect）→ マップ選択（MapSelect）を経て Main へ進み、**CPU とのすごろく対戦**を行う（プレイ人数は MapSelect で **2〜4 人**を選ぶ＝自分 1 人＋残りが CPU。あなたが先攻、以降交互）。**オンラインプレイはマッチング成立（満室）後、キャラ選択ロビー（`OnlineCharacterSelect`）を経て Main へ進む**：全員が使うキャラを選び、**被らない仕組み**として UGS のプレイヤープロパティ／セッション共有プロパティでホストが審判し先着ロックする（他プレイヤーが選んだキャラはグレーアウトして選べない・`CharacterClaimResolver` が純粋ロジック）。全員が「決定」して割り当てが揃うと、席順→キャラの割り当てを `OnlineRosterSessionModel` に保持して Main へ遷移する（NGO は使わず UGS セッションのポーリング同期・`CharacterLobbySync`）。**盤面（マップ）は複数用意でき、`BoardCatalog` にまとめて MapSelect で 1 つ選ぶ**（選択は Common の `BoardSessionModel` に識別子で保持し `BoardPresenter` がカタログから解決。**オンラインはホストがルーム作成時に選ぶ**＝マッチング画面の「変更」で MapSelect と同じ見た目〔サムネイル一覧＋大プレビュー＋イベント内訳〕の全画面マップ選択オーバーレイを開いて選び、`CharacterLobbySync` の共有状態〔`lobbyState.board`〕で全員へ同期する。カード一覧・プレビュー・内訳・選択のロジックは MapSelect と共通の `MapPickerView` に切り出して共用）。**円盤ルーレットで「誰が何マス進むか」を決める**（ボタンを長押し中は回転し、離すと減速して止まった位置のセクターで確定。CPU の番は同じ円盤が自動で回る）。**円盤のセクターには参加者（自分＋CPU）を均等に割り振り**（`セクター i → 参加者 i % 人数` のラウンドロビン・ゲーム開始時に固定＝`RouletteMath.ParticipantForSector`）、**止まったセクターのキャラがそのセクターの数字ぶん進む**（＝進む人はルーレット任せで、手番＝スピンする人とは別。自分に止まれば自分が、CPU に止まればその CPU が進む）。**各参加者は同じ数字セット 1〜K を 1 枚ずつ持つ**（`セクター i の数字 = (i ÷ 人数) + 1`＝`RouletteMath.StepsForSector`・数字も全キャラ同じ・K は `RoulettePresenter._numbersPerCharacter`＝既定3・セクター総数 = 人数 × K）。外周マスを並べたループ盤面で、進むプレイヤーのコマが出目ぶん進む（周回勝利は廃止したのでスタート＝ゴールを通過して回り続ける）。盤面は左下の虫眼鏡 +／− ボタンで**表示列数を段階ズーム**でき（既定4列・拡大で3→2列・縮小で6→8列）、画面外へはみ出したぶんは**ドラッグでパン**して見る（横長マップ向け・`BoardZoomController`）。**勝敗は陣地マス（`BoardCellEvent.Territory`）の占拠で決まる**：止まったマスを占拠し（相手の陣地でも上書きで奪える）、盤面の陣地マス総数を**プレイヤー数で割った数（端数切り上げ）を先に占拠した方が勝ち**（`TerritoryModel` が状態と勝利判定を持つ）。陣地マス着地時は**そのコマのキャラの旗を画面中央に 1 秒表示→縮小しながらマスへ重ねて占拠する演出**を再生し、占拠後そのマスは territory 画像ではなく所有者の旗画像のままになる（**ただし止まったマスをすでに自分が占拠している場合は占拠状態が変わらないので旗演出をスキップする**）。**勝敗が確定すると盤面下部に「ホームに戻る」ボタンが出て、押すと Home へ戻れる**。**決着時は AssetStore のパーティクル Prefab を画面前面で再生する：自分（人間プレイヤー）が勝ったら花火（既定は CFXR の花火）、負けた（CPU が勝った）ら雨（既定は CFXR4 Rain Falling）**（`ScreenEffectPlayer`＝任意 Prefab を前面再生する汎用クラス・勝利用/敗北用で別インスタンス。UI Toolkit の ScreenSpaceOverlay はワールド空間のパーティクルを覆い隠すため、専用カメラ→RenderTexture→加算ブレンド Canvas の RawImage で前面合成する＝[docs/effects.md](docs/effects.md) の方式）。手番進行は `GameFlowController` が統括する（オンラインは**ルーム定員ぶん＝2〜4 人**〔ホストがルーム作成時に選ぶ・単独プレイは廃止〕。キャラの割り当ては上記キャラ選択ロビーで同期して各席を被らないキャラで描画する。**盤面の進行〔手番・出目・コマ移動・お金・アイテム・勝敗〕も `OnlineGameSync` のアクションストリームで同期する**＝進行を進める「決定」を `GameAction` にして、決めた 1 人が発行 → ホストが順序付け役として全員へ再配信 → **決めた本人も含め受信したアクションだけを適用**する。一人用モードも同じ経路を通る〔発行が即ローカルのキューへ積まれるだけ〕。詳細は [docs/networking.md](docs/networking.md)「ゲーム進行の同期」。**NGO は Relay 経由で繋がるので離れた相手とも対戦できる**〔`NetworkManager` は Common 常駐・NGO の起動と停止は UGS セッションが握る〕＝[docs/networking.md](docs/networking.md)「Relay 経由の接続」）。プレイヤーは**所持金**を持ち（`MoneyModel`・初期 1000・マイナス可）、お金アップ/ダウンのマスに止まると増減する（**増減額はマスごとの固定値ではなく着地のたびに n×100〔n=1〜5〕のランダム**＝`MoneyCellRule`。増える/減るの符号だけがマスのイベント種別で決まる。盤面上部の自分ネームプレートに表示）。**アイテム取得マス（`BoardCellEvent.Item`）**に止まると**アイテムショップ**が開き、`ItemCatalog` からランダムな枚数（2〜4枚・重複なし＝`RandomLineup`）を抽選したラインナップを**所持金で購入**する（`ItemShopPresenter`。各商品カードに絵・名前・効果説明・価格を表示し、**一度に2枚だけ見せ、残りは前後ボタン＋ドットのカルーセルでページ送り**する。価格はアイテム種類ごとに固定＝`ItemDefinition.Price`。所持金で買えないカードは無効表示。買わずに閉じてもよい〔誤タップ防止で暗幕クリックでは閉じず「買わずに閉じる」ボタンでだけ閉じる〕。**CPU も買える範囲でランダムに1枚自動購入**する〔手札 UI は非表示のまま〕）。購入すると代金を `MoneyModel` から支払い〔購入音＝`MoneySE`〕、買ったアイテムを**画面右下の手札**にサムネイル追加する（`ItemModel` がプレイヤーごとの手札を保持・表示は自分＝人間プレイヤーのぶんのみ・CPU は非表示。**同じアイテムを重ねて取ったときはカードを増やさず、既存カード右下に「x2」の枚数バッジ**を出す）。手札のアイテムは**クリックで詳細モーダル**（絵・名前・効果説明＋「使用する」「閉じる」）が開き、「使用する」で効果ハンドラ（`onUse`）が呼ばれる（`ItemModalPresenter` は消費せず `onUse` を呼ぶだけで、消費〔`ItemModel.Use`〕と効果発動は `BoardPresenter.HandleItemUse` が担う。**「使用する」は自分の手番かつルーレット未回転〔`Idle`〕でアイテム効果の実行中でないときだけ有効**で、回した後・コマ移動中・相手の手番中は無効）。**「陣地獲得」は効果実装済み**＝使用すると自分以外が持つ陣地マス（未占拠＋相手占拠）が金枠＋キラキラで強調され、盤面タップ〔ドラッグでパンも可・上部にガイド＋キャンセル〕で 1 つ選んで占拠する（既存の旗演出→占拠確定→必要数に達したら勝利。選択のキャンセルでは消費しない。効果はターンを消費せず使用後もルーレットを回せる。選択・演出中はスピンボタンを無効化）。**「ミニゲーム」も効果実装済み**＝使用すると遊ぶミニゲームを選ぶモーダル（`MiniGameSelectPresenter`＝`MiniGameCatalog` をサムネイル画像＋ゲーム名のカード一覧）が開き、選んだミニゲームを `MiniGameLauncher` で起動して勝てば所持金報酬（`MoneyModel.Add`・既定 +500）をもらえる（2Dレースは先着で勝ち・タップ連打は他の参加者（CPU も自動連打）より多く連打して 1 位なら勝ち・被っちゃやーよは他の誰とも被らないアイテムを選べば勝ち。キャンセルでは消費しない。ターン非消費で選択・プレイ中はスピン無効）。**「お金よこどり」も効果実装済み**＝使用すると自分以外の参加者（一人用なら CPU・オンラインなら他プレイヤー）の所持金の一部（現在の所持金の20〜50％をランダム・`MoneyStealRule`）を奪って自分に足し、増額を中央の浮遊テキストで見せる（相手の所持金が正のときだけ発動＝奪える額が無ければ消費しない。相手の所持金もネームプレートに出るので減るのは見えるが、演出は自分側のみ。ターン非消費で演出中はスピン無効）。**「勝利」も効果実装済み**＝使用した瞬間に `BoardModel.SetWinner` で自分の勝ちを確定する（一発逆転。マス選択などの操作は無く即時発動し、勝者表示・「ホームに戻る」ボタン・花火エフェクトは通常の勝利と同じ流れで再生される）。ミニゲーム（タップ連打／CPU（複数可）と競うタイミングメーター式の2Dレース／参加者数ぶんのアイテムから被らない1枚を選ぶ「被っちゃやーよ」）は Main を残したまま MiniGame シーンを Additive で重ねて起動する仕組み（中身は Addressables 差し替え式で将来最大5種類）。**ゲーム内からの起動は 2 経路**＝**ミニゲームマス（`BoardCellEvent.MiniGame`）への着地**〔マスごとに種類を設定・そのゲームのサムネイルがマスの絵になる〕と、**ミニゲームアイテム**〔遊ぶゲームをモーダルで選ぶ〕。**オンラインでは部屋の全員が同時に遊んで実スコアで順位を決める**〔着地／使用した人が「内容を組み立てる種」を配って全員が同じゲームを開き、各自が自分の結果値を `GameAction.MiniGameScore` で出し合う。勝者は純粋関数 `MiniGameRanking.Resolve` が決めるので判定役が要らない＝連打は最多／レースは最短タイム／被っちゃやーよは誰とも被らない・同点は全員勝ち。**タップ連打だけは互いの連打数を 200ms ごとに配り合ってスコアボードに出す**〔見た目だけの情報なのでアクションストリームには載せず別経路〕。勝者は所持金 +500 で、盤面上部の帯で勝者を発表する〕。一人用モードは相手をゲーム内の CPU がシミュレートする〔`MiniGameSessionModel.SimulateOpponents`〕。動作確認は専用の **MiniGameTest シーン**でも行える（本番フローには出さず、エディタで直接開いて Play する。カタログのミニゲームがボタンで自動一覧される）。

## Unity 開発

ビルドと実行は Unity Editor (Unity 6000.3.18f1) を通じて行う。独立したビルドスクリプトは存在しない。

- **テスト実行 (EditMode)**: Unity Editor → Window → General → Test Runner → EditMode タブ → Run All
- **テスト実行 (PlayMode)**: Unity Editor → Window → General → Test Runner → PlayMode タブ → Run All
- **ビルド**: Unity Editor → File → Build Settings → Build

## テスト構成

| ディレクトリ | 種別 | 内容 |
|---|---|---|
| [Assets/Tests/PlayMode/](Assets/Tests/PlayMode/) | PlayMode | シーンロードを伴う統合テスト |
| [Assets/Tests/EditMode/](Assets/Tests/EditMode/) | EditMode | 純粋ロジックの単体テスト |

**PlayMode テストの注意点:**
- `CommonSceneLoader` が `static bool _loaded` を持つため、`[UnityTearDown]` で reflection リセットが必要
- `IAsyncStartable.StartAsync` は VContainer からキャンセルトークンを受け取るため `catch (OperationCanceledException)` で正常終了させること
- ボタンクリック模擬は `NavigationSubmitEvent`（`ClickEvent` では Clickable が反応しない）
- 動画を再生するシーン（Title 等）を丸ごとロードするテストは、再生不可の環境で `VideoPlayer` がネイティブに吐く `Error` ログだけでテストが失敗扱いになる。動画の成否を検証しないテストでは `SetUp`/`TearDown` で `LogAssert.ignoreFailingMessages` を切り替えて回避する（本来の不具合は各アサーションで検出される）

**EditMode テストの注意点:**
- asmdef の `references` に、テスト対象クラスのアセンブリ GUID とその直接依存アセンブリ GUID を追加する（推移的参照は自動解決されない）
- `R3.dll` を `precompiledReferences` に追加が必要
- `ReadOnlyReactiveProperty<T>` の値は `.CurrentValue`（`.Value` は不可）。`ReactiveProperty<T>` は `.Value` で読み書き可
- R3 の Subscribe 拡張メソッドには `using R3;` が必要
- UniTask の同期完了タスクは `.GetAwaiter().GetResult()` でテスト可能（null ガード等の即完了ケース）

## アーキテクチャ

詳細は [docs/architecture.md](docs/architecture.md) を参照。

要点:
- `Common` シーンが常駐し、他シーンはアディティブでロード
- DI は VContainer（`Find()` / static 禁止）
- 状態管理は R3 の `ReactiveProperty<T>`（Model → Presenter の単方向フロー）
- アセットは Addressables（`Resources.Load` 禁止）。例外として**動画は StreamingAssets に置き `VideoPlayer` の URL で再生する**（WebGL は `VideoClip` アセット非対応のため。タイトル動画がこの方式）
- UI は UI Toolkit / UXML + USS（uGUI 禁止）。スタイルはインラインでなく USS ファイルに定義してクラスで適用する

  - 新しいシーンを作成するときは右上エリアに UI 要素を配置しない（Common シーンのオプションアイコンが `right:0 / top:0` に重なるため）。詳細は [docs/design-system.md](docs/design-system.md) を参照

## コーディング規約 (.editorconfig でエラーとして強制)

- **命名**: 型・メソッド・プロパティ・定数は `PascalCase`、フィールドは `_camelCase`、引数・ローカル変数は `camelCase`
- **明示的な型を優先** (`var` は使用しない。`csharp_style_var_*` はすべて false)
- **アクセス修飾子必須** (インターフェースメンバー以外のすべてのメンバー)
- **readonly フィールド**を可能な限り使用
- すべてのブロックに波括弧必須、開き波括弧は新しい行に配置
- `using` ディレクティブは名前空間の外側に記述し、System ディレクティブを先頭に並べる

## 主要ファイルの場所

| 用途 | パス |
|---|---|
| Common DI 登録 | [Assets/Scripts/Common/Injector/CommonLifeTimeScope.cs](Assets/Scripts/Common/Injector/CommonLifeTimeScope.cs) |
| シーン遷移ロジック | [Assets/Scripts/Common/SceneManagement/SceneTransitioner.cs](Assets/Scripts/Common/SceneManagement/SceneTransitioner.cs) |
| シーン準備完了通知（任意実装。非同期初期化の完了を待ってからフェードイン） | [Assets/Scripts/Common/SceneManagement/ISceneReady.cs](Assets/Scripts/Common/SceneManagement/ISceneReady.cs) |
| 遷移演出 | [Assets/Scripts/Common/Transition/TransitionPresenter.cs](Assets/Scripts/Common/Transition/TransitionPresenter.cs) |
| サウンド再生 | [Assets/Scripts/Common/SoundManagement/SoundPlayer.cs](Assets/Scripts/Common/SoundManagement/SoundPlayer.cs) |
| ボリューム状態モデル | [Assets/Scripts/Common/Option/OptionModel.cs](Assets/Scripts/Common/Option/OptionModel.cs) |
| オプションモーダル UI バインド | [Assets/Scripts/Common/Option/OptionModalPresenter.cs](Assets/Scripts/Common/Option/OptionModalPresenter.cs) |
| Store 共通基底クラス | [Assets/Scripts/Common/Store/AssetStoreBase.cs](Assets/Scripts/Common/Store/AssetStoreBase.cs) |
| セッション保持・ゲームモード（Common） | [Assets/Scripts/Common/GameSession/GameSessionModel.cs](Assets/Scripts/Common/GameSession/GameSessionModel.cs) / [GameMode.cs](Assets/Scripts/Common/GameSession/GameMode.cs) |
| 参加者リスト（GameMode から生成。一人用=[Human, Cpu×(人数-1)]＝人数は `PlayerCountSessionModel.Count`〔MapSelect で選ぶ 2〜4〕、オンライン=[Human×N]＝ルーム定員ぶん〔2〜4・ホストがルーム作成時に選ぶ・`GameSessionModel.SessionMaxPlayers`＝`ISession.MaxPlayers` を `OnlinePlayerCountFrom` で下限2にクランプ・単独プレイは廃止〕）・プレイヤー種別 | [Assets/Scripts/Main/Turn/GameParticipants.cs](Assets/Scripts/Main/Turn/GameParticipants.cs) / [PlayerKind.cs](Assets/Scripts/Main/Turn/PlayerKind.cs) |
| プレイ人数保持（Common。一人用モードの人数〔自分＋CPU〕をシーンをまたいで保持。`Min=2`／`Max=4`・`Select` でクランプ。MapSelect で選び `GameParticipants` が参加者生成に使う。オンラインは未使用） | [Assets/Scripts/Common/GameSession/PlayerCountSessionModel.cs](Assets/Scripts/Common/GameSession/PlayerCountSessionModel.cs) |
| プレイヤー色（コマ・陣地占拠マス・ネームプレート上辺を p0〜p7 の 8 色で色分けする共通ヘルパ。index を色数にクランプ。色定義は Board.uss の `board-piece--p{N}`／`board-cell--owned-p{N}`／`board-nameplate--p{N}`） | [Assets/Scripts/Main/Board/PlayerColors.cs](Assets/Scripts/Main/Board/PlayerColors.cs) |
| CPU のキャラ解決（人間は選択キャラ、CPU は人間とも他 CPU とも被らないキャラをシャッフルして 1 度だけ配って固定。抽選本体は純粋関数 `ShuffledNonHumanIndices`。**DI で Scoped 登録して `BoardPresenter`〔コマ・ネームプレート〕と `RoulettePresenter`〔セクターのキャラ〕で共有**＝盤面とルーレットで CPU の割り当てを一致させる。**オンライン時（`GameMode.Online` かつロースター有り）はキャラ選択ロビーで確定した `OnlineRosterSessionModel` の席順→キャラを全席に使う**＝全プレイヤーが被らないキャラで描画される） | [Assets/Scripts/Main/Board/CpuCharacterPicker.cs](Assets/Scripts/Main/Board/CpuCharacterPicker.cs) |
| オンラインのキャラ割り当て保持（Common。キャラ選択ロビーで確定した席順→`CharacterId` と自分の席 index をシーンをまたいで保持する `CharacterSessionModel` のオンライン多人数版。`HasRoster`／`MySeat`／`CharacterAt(seat)`／`SetRoster`／`Clear`。Main の `CpuCharacterPicker`〔各席のキャラ〕と `BoardPresenter`〔自分の席＝人間プレイヤー〕が参照。一人用では未設定〔`HasRoster` false〕） | [Assets/Scripts/Common/GameSession/OnlineRosterSessionModel.cs](Assets/Scripts/Common/GameSession/OnlineRosterSessionModel.cs) |
| オンライン キャラ選択ロビー（マッチング満室後・Main の前。全員が被らないキャラを選ぶ。**入室時は自分の席順〔参加順〕ごとの初期キャラ`CharacterCatalog.DefaultFor(seat)`が選択済みで開く**〔席ごとにずらすので初期状態から誰とも被らず、そのまま「決定」するだけでも成立する。席は `CharacterClaimResolver.SeatIndexOf`＝確定ロースターと同じ参加順で求める〕。カード一覧＋選択で立ち絵を全画面背景表示〔オフラインの CharacterSelect と同じ遅延ロード＋キャッシュ・初期キャラの立ち絵は表示前にロード〕・他プレイヤーがロックしたキャラはグレーアウト＋「取得済み」バッジ・自分の選択＝黄枠／「決定」で緑枠〔往復を待たず自分の画面は即時＝楽観表示・競合で負けたら集計で自動解除〕・**全員がロビーに到着するまでは選択・決定できない**〔`CharacterLobbySync.AllPresent`。到着に差が出るので先に選ぶとロックを集計してもらえず待ち続けてしまう。待機中は「他のプレイヤーの参加を待っています...（◯/◯人）」を表示〕・「退出」でセッション離脱→Title。`ISceneReady` で表示前にカード絵を並列ロード。UI 構築後に `CharacterLobbySync.RunLoopAsync` を起動） | [Assets/Scripts/OnlineCharacterSelect/Presenter/OnlineCharacterSelectPresenter.cs](Assets/Scripts/OnlineCharacterSelect/Presenter/OnlineCharacterSelectPresenter.cs) |
| キャラ選択ロビーの状態同期（UGS セッションのプレイヤープロパティ〔各自の希望キャラ`char`・決定`ready`〕とセッション共有プロパティ〔ホストが書くロック表＋開始ロースター＋**ホストが選んだマップ識別子**`lobbyState`〕で「被らないキャラ選択」を成立させる。**ホストが審判**＝`CharacterClaimResolver` で先着ロックを決めて全員へ共有。反映は `MatchingService.WaitForPlayerAsync` と同じくライブなセッションのポーリング〔600ms・`RefreshAsync` 不要〕。**到着人数〔`PresentCount`／`AllPresent`＝自分のプレイヤープロパティを 1 度でも書いた人数が定員に達したか〕も数え、全員そろうまで `Select`／`Confirm` を受け付けない**〔ロビーへの到着はシーン遷移とカード絵ロードのぶん差が出る。先に選ぶとロックを集計してもらえないまま待ち続けてしまうため〕。**ホストは自分の `BoardSessionModel.SelectedId` を `lobbyState.board` に載せ、ゲストは開始確定時に `BoardSessionModel.Select` で反映する**〔全員が同じ盤面で Main へ・空ならカタログ既定〕。ロビー確定で `OnlineRosterSessionModel.SetRoster` に保存し `Started` を発火して Main へ遷移。NGO は不使用〔NetworkManager は Main のみ〕。UGS の await 後は非メインスレッドになり得るため Reactive/UI 更新前に `SwitchToMainThread`） | [Assets/Scripts/OnlineCharacterSelect/Sync/CharacterLobbySync.cs](Assets/Scripts/OnlineCharacterSelect/Sync/CharacterLobbySync.cs) / [CharacterClaimResolver.cs](Assets/Scripts/OnlineCharacterSelect/Sync/CharacterClaimResolver.cs) |
| 手番状態（現在の手番プレイヤーの巡回） | [Assets/Scripts/Main/Turn/TurnModel.cs](Assets/Scripts/Main/Turn/TurnModel.cs) |
| ターン進行の統括（手番巡回のオーケストレータ。散在していた「ルーレット停止→前進」の連鎖をここへ集約。**手番プレイヤー＝スピンする人だが、前進させるのはルーレットが止まったキャラ＝「進む人」**。**進行は `OnlineGameSync` のアクションストリームで駆動する**＝手番の席を担当するクライアントだけがスピンを駆動し〔`DriveSpinAsync`〕、**回し始め〔`SpinStart`〕と停止位置〔`Spin`＝セクター＋減速時間〕の 2 段で発行する**〔停止位置は円盤が止まるのを待たず「押下を離した瞬間」に確定＝`SpinDecision`・`RouletteModel.Decided`〕。全員が受信したセクターから進む人とマス数を復元して適用する〔自分で回していない側は `SpinStart` で一緒に回り始め〔`RoulettePresenter.BeginRemoteSpin`〕、`Spin` を受けて同じセクター・同じ減速時間で止める〔`PlaySpinToAsync`〕＝相手が回している間も画面が止まらず結果もほぼ同時に出る〕。スピンを待つ間に届いたアイテム使用は `BoardPresenter.ApplyActionAsync` へ流して適用する。進行フローの詳細は [docs/architecture.md](docs/architecture.md)「シーン構成」） | [Assets/Scripts/Main/Turn/GameFlowController.cs](Assets/Scripts/Main/Turn/GameFlowController.cs) |
| ゲーム進行の同期（アクションストリーム。進行を進める「決定」を `GameAction`〔種別＋発行席＋整数パラメータの値型・`GameActionCodec` で JSON 化〕にして、決めた 1 人が `Publish` → ホストが唯一の順序付け役として全員へ再配信 → **決めた本人も含め受信したアクションだけを適用**する。これで全クライアントの適用順が一致する。一人用モードも同じ経路を通る〔`Publish` が即ローカルのキューへ積まれるだけ〕ので進行のコードパスが一本化する。**相手の操作を待っている間は画面上部に待機表示〔`WaitingBanner`〕を出す**〔「〔キャラ名〕が買い物中…」のように末尾の「.」が動く。モーダル操作やミニゲームのように決める人の手元で時間がかかる処理は `Busy`〔`BusyReason`＝買い物／ミニゲーム／陣地選択〕を配って知らせ、盤面は進めないお知らせなので受信側は表示を切り替えて待ち続ける・結果のアクションが届けば自動で消え、キャンセル時だけ `Busy(None)` を配る。**相手の手番でまだルーレットを回していない間の「〔キャラ名〕のルーレット待ち…」と、アイテムショップの「買い物中」は配らずローカルで導出**〔手番・ルーレット状態・着地マスはどれも全員が同じ値を持つ〕。優先度は `Busy` ＞ ルーレット待ちで、コマ移動中・着地演出中・アイテム効果中・決着後・退出後は出さない。一人用は全席をローカルが決めるので出ない〕。受信は `ActionStream` にバッファして送受信の前後関係に依存しない〔[docs/networking.md](docs/networking.md) 8 の恒久策〕・**同時に待てるのは 1 箇所だけ**〔手番待ち＝`GameFlowController`／着地待ち＝`BoardPresenter` が所有権を受け渡す〕。`IsLocalDecider(seat)` で「その席の決定を自分がするか」を判定〔オンライン＝自席のみ・一人用＝全席〕。切断は `NetworkManager.OnClientDisconnectCallback` で検知し、ゲスト同士には伝わらないためホストが `Leave` を配る→`SessionLost` で進行を打ち切り `BoardPresenter` が「相手が退出しました」を出す。接続は Relay 経由〔`SessionOptions.WithRelayNetwork()`〕なので離れた相手とも繋がる） | [Assets/Scripts/Main/Online/OnlineGameSync.cs](Assets/Scripts/Main/Online/OnlineGameSync.cs) / [GameAction.cs](Assets/Scripts/Main/Online/GameAction.cs) / [GameActionCodec.cs](Assets/Scripts/Main/Online/GameActionCodec.cs) / [ActionStream.cs](Assets/Scripts/Main/Online/ActionStream.cs) / [BusyReason.cs](Assets/Scripts/Main/Online/BusyReason.cs) |
| モード選択（Home の一人用/オンライン分岐。一人用は CharacterSelect へ。クレジットモーダルの開閉も担当。**固定画像 `Image/HomeBackGround` を全画面背景に表示**し、上に暗いスクリムを重ねて前面 UI の視認性を確保。画像は Addressables ロード・未配置は暗い地色のプレースホルダ。表示前にロードを終えるため `ISceneReady` を実装。3 ボタン〔一人用モード・オンラインプレイ・クレジット〕は同じアクセント色〔`btn-accent`〕・タイトルロゴは無し） | [Assets/Scripts/Home/Presenter/HomePresenter.cs](Assets/Scripts/Home/Presenter/HomePresenter.cs) |
| キャラ識別子・カタログ・選択状態（Common。シーンをまたいで保持。各キャラは Card（選択カード絵）/Icon（盤面コマの丸バッジ）/Portrait（立ち絵）/Run（2Dレースの走行絵）/Flag（陣地マス占拠時の旗絵・`Character/<名前>/Flag`）の5系統の Addressable アドレスを持つ。**席順ごとの初期キャラは `CharacterCatalog.DefaultFor(seat)`＝カタログの表示順そのまま**〔1P=のらどっく / 2P=ザニザニマン / 3P=D.O.M / 4P=アリマ・範囲外はクランプ〕なので、席が違えば初期キャラも被らない。`CharacterSessionModel` の初期値も 1P のぶん〔`DefaultFor(0)`〕） | [Assets/Scripts/Common/Character/CharacterId.cs](Assets/Scripts/Common/Character/CharacterId.cs) / [CharacterCatalog.cs](Assets/Scripts/Common/Character/CharacterCatalog.cs) / [CharacterSessionModel.cs](Assets/Scripts/Common/Character/CharacterSessionModel.cs) |
| キャラ選択 UI（立ち絵を全画面背景・カード絵の選択スロットを下部に配置。戻る／決定ボタンは画面上部（右上のオプションアイコンを避けて中央寄せ）。キャラ名は各カード内に表示。画像は `CardAddress`（カード）と `PortraitAddress`（立ち絵）を Addressables ロード、未配置は色面プレースホルダ。フェードイン前（`ReadyAsync`）はカード絵を全キャラぶん `UniTask.WhenAll` で**並列ロード**し、立ち絵は**選択時に遅延ロード＋キャッシュ**（フェードイン前に待つのは初期選択キャラの立ち絵1枚だけ＝遷移待ちを短縮。ロード中に選択が変わったら結果は破棄）。決定でキャラを保存し MapSelect へ遷移） | [Assets/Scripts/CharacterSelect/Presenter/CharacterSelectPresenter.cs](Assets/Scripts/CharacterSelect/Presenter/CharacterSelectPresenter.cs) |
| マップ一覧カタログ（`BoardDefinition` は SO 資産で静的クラスから参照できないため、キャラの `CharacterCatalog` と違いカタログ自身も SO。`List<BoardDefinition>` を持ち `All`/`Default`/`Find(識別子)` を公開。識別子＝マップ資産名（`Object.name`）。`BoardCatalog.asset` を作り MapSelect の Presenter と Main の `BoardPresenter` の両方へインスペクタで割り当てる） | [Assets/Scripts/Main/Board/BoardCatalog.cs](Assets/Scripts/Main/Board/BoardCatalog.cs) |
| 選択マップ保持（Common。シーンをまたいで保持する `CharacterSessionModel` のマップ版。Common から Main の `BoardDefinition` を参照できないため識別子＝マップ資産名の文字列だけを持つ。実体解決はカタログ側） | [Assets/Scripts/Common/Board/BoardSessionModel.cs](Assets/Scripts/Common/Board/BoardSessionModel.cs) |
| マップ選択 UI（`BoardCatalog` のマップを盤面サムネイル付きカードで一覧し、選ぶと大プレビューに拡大。**プレイヤー人数〔自分＋CPU・2〜4〕を −／＋ ステッパーで選ぶ**〔下限/上限でボタン無効化・決定時に `PlayerCountSessionModel` へ保存〕。決定で `BoardSessionModel`〔＋人数〕を保存して Main へ・戻るで CharacterSelect へ。**カード一覧・大プレビュー・イベント内訳・選択状態は共通の `MapPickerView`〔Main〕に委譲する**〔オンラインのルーム作成マップ選択と共用〕。サムネイルは画像を使わず `BoardSchematicView` が Painter2D で盤面の形を描き、**各マスをイベント種別ごとの色〔`BoardEventColors`・盤面エディタと共通の配色〕で塗り分ける**〔スタート＝index 0 は金色〕ので、どんなイベント構成のマップかをサムネイルだけで見分けられる。**大プレビューの下には選択中マップのイベント内訳〔総マス数＋「色ドット＋ラベル ×N」のチップ〕を表示**する〔陣地・アイテム・お金…がどれだけあるかをひと目で把握。集計は純粋関数 `BoardEventTally.Summarize`＝スタート/通常マスを除外し陣地を先頭に表示順で並べる・ラベルは `BoardEventLabel`〕。マップ名は `BoardDefinition.DisplayName`〔空なら資産名〕。UI 構築は注入後に走る `ISceneReady.ReadyAsync` で行う） | [Assets/Scripts/MapSelect/Presenter/MapSelectPresenter.cs](Assets/Scripts/MapSelect/Presenter/MapSelectPresenter.cs) / [MapSelectLifetimeScope.cs](Assets/Scripts/MapSelect/Injector/MapSelectLifetimeScope.cs) / [MapSelect.uxml](Assets/Scripts/MapSelect/View/MapSelect.uxml) |
| マップ選択の共通 UI コントローラ（`BoardCatalog` のマップを盤面サムネイル〔`BoardSchematicView`〕付きカードでグリッド一覧し、選ぶと大プレビュー・タイトル・イベント内訳〔`BoardEventTally`／`BoardEventColors`／`BoardEventLabel`〕を更新する。選択状態を保持し `SelectedId`／`HasSelection`／`Selected`〔選択変化イベント〕を公開・`Build(catalog, initialId)` で組む。**MapSelect シーン〔`MapSelectPresenter`〕とオンラインのルーム作成マップ選択〔`MatchingPresenter` の全画面オーバーレイ〕で共用**。カード/プレビュー/チップの USS クラスは共通〔`map-card` 等〕なので埋め込む側の USS に同じクラスを定義する。盤面サムネイルの Painter2D 描画は `BoardSchematicView`〔Main 型のみ依存〕） | [Assets/Scripts/Main/Board/MapPickerView.cs](Assets/Scripts/Main/Board/MapPickerView.cs) / [BoardSchematicView.cs](Assets/Scripts/Main/Board/BoardSchematicView.cs) |
| 盤面イベントの色・ラベル・集計（ランタイム共通ヘルパ。`BoardEventColors.Of`＝イベント種別→色／`BoardEventLabel.Of`＝イベント種別→日本語名を単一の情報源として持ち、盤面エディタ〔`Main.EditorTools` の `EventColor`／`EventLabel`〕とマップ選択のサムネイル・内訳が同じ配色・文言を使う。`BoardEventTally.Summarize`＝盤面のイベント構成を数える純粋関数〔スタート＝index 0 と通常マス〔`None`〕を除外し、陣地→アイテム→ミニゲーム→お金→移動系の表示順で 1 個以上あるものだけ返す〕） | [Assets/Scripts/Main/Board/BoardEventColors.cs](Assets/Scripts/Main/Board/BoardEventColors.cs) / [BoardEventLabel.cs](Assets/Scripts/Main/Board/BoardEventLabel.cs) / [BoardEventTally.cs](Assets/Scripts/Main/Board/BoardEventTally.cs) |
| マッチングサービス（UGS Session API 呼び出し。**ルーム作成時に `SessionOptions.WithRelayNetwork()` を付ける**＝SDK が Relay の割り当てと NGO の `StartHost`/`StartClient` まで面倒を見るので、アプリ側から NGO を起動・停止してはいけない〔停止は `ISession.LeaveAsync`〕。参加側はホストが公開した join code で自動接続するのでオプション不要。`NetworkManager` はセッション作成時に必要なので **Common シーンにルートオブジェクトとして常駐**させる。詳細は [docs/networking.md](docs/networking.md)「Relay 経由の接続」） | [Assets/Scripts/Matching/MatchingService.cs](Assets/Scripts/Matching/MatchingService.cs) |
| マッチング画面 UI・フロー（ルーム一覧の閲覧／参加・ホストのルーム作成・相手待ち。`MatchingPresenter` が UI バインド、`MatchingFlow` がフロー制御〔認証・自動更新・作成/参加・待機・開始〕。**満室になったら `StartGameAsync` がキャラ選択ロビー〔`OnlineCharacterSelect`〕へ遷移するが、ホストだけ `HostStartDelayDuration`〔3秒〕待ってから遷移する**〔ホストの方が先に満室を検知しがちなのでゲストを先に到着させる〕。**ホストはルーム作成パネルで定員〔2〜4〕とマップを選ぶ**：マップは「マップ：名前＋変更」の行で、「変更」を押すと MapSelect 風の**全画面マップ選択オーバーレイ**〔`MapPickerOverlay`。中身は共通の `MapPickerView`〔Main〕・確定＝「このマップにする」／破棄＝「閉じる」〕を開いて選ぶ。作成時に選んだ識別子を `MatchingFlow.CreateRoomAsync(maxPlayers, boardId)` へ渡し `BoardSessionModel.Select` に保持〔`CharacterLobbySync` でゲストへ同期〕。**作成したルームには選んだマップ識別子を公開セッションプロパティ〔キー `MatchingService.BoardPropertyKey="board"`・`VisibilityPropertyOptions.Public`〕として載せる**ので、`QuerySessionsAsync` の結果〔`ISessionInfo.Properties`〕経由でルーム一覧を閲覧するゲストにも参加前にマップ名が見える〔`MapSessionsToRooms`→`LobbyInfo.BoardId`→`MatchingPresenter` が `BoardCatalog` で表示名に解決してルームボタンに「マップ：〇〇」と表示〕。`BoardCatalog` は Matching シーンの `MatchingPresenter` にインスペクタ割り当て〔未割り当て時はマップ選択・作成を無効化〕。Matching アセンブリは `BoardCatalog`／`MapPickerView` 参照のため Main を参照する） | [Assets/Scripts/Matching/MatchingPresenter.cs](Assets/Scripts/Matching/MatchingPresenter.cs) / [MatchingFlow.cs](Assets/Scripts/Matching/MatchingFlow.cs) |
| マッチング DI 登録 | [Assets/Scripts/Matching/Injector/MatchingLifetimeScope.cs](Assets/Scripts/Matching/Injector/MatchingLifetimeScope.cs) |
| NGO の接続待ち（**NGO の起動・停止はしない**＝オンラインの接続は UGS セッションが Relay 割り当てと一緒に張り〔`WithRelayNetwork()`〕、`ISession.LeaveAsync` で閉じる。ここは接続が整うのを待つだけ。整ったら `Connected` 通知の**前**に `OnlineGameSync.OnConnected()` でメッセージハンドラを永続登録する＝最初のアクションから取りこぼさない。一人用は NGO を使わないので即 `Connected`） | [Assets/Scripts/Main/NetworkSessionStartup.cs](Assets/Scripts/Main/NetworkSessionStartup.cs) |
| NGO メッセージ送受信（名前付きメッセージを JSON 文字列で送受信する薄いラッパー。`SendJson`〔指定クライアントへ〕／`SendJsonToOthers`〔自分以外の全員へ＝ホストの再配信用・除外 id 指定可〕／`RegisterJson`〔受信ハンドラ登録〕／`Unregister`。ペイロードの型付けは上位層〔`GameActionCodec`〕に任せる） | [Assets/Scripts/Main/NgoMessenger.cs](Assets/Scripts/Main/NgoMessenger.cs) |
| ルーレットの停止角度→セクター変換・状態（出目・進む参加者は止まった位置で決まる）・セクターへの割り当て純粋関数（`SectorCount`＝人数×K・`ParticipantForSector`＝参加者を巡回で均等配置・`StepsForSector`＝各キャラが同じ数字 1〜K を1枚ずつ持つ番号付け〔**セクター ↔ 〔進む人, 出目〕は 1 対 1** なのでオンラインは停止セクターの整数 1 つを配れば全員が同じ結果を復元できる〕・`RotationForSectorCenter`／`NextRotationFor`〔指定セクターの中心で止まる回転角。受信側の円盤再生に使う〕・`NearestRotationForSectorCenter`〔自然減速の停止予測角をいちばん近いセクター中心へ寄せる＝**離した瞬間に止まるセクターを確定させる**ために使う〕）。`RouletteModel` は出目（`Result`）に加え**進む参加者**（`AdvancingPlayer`）を保持し `CompleteSpin(steps, player)` で確定。**減速に入った時点の確定は `Decided`〔`Observable<SpinDecision>`＝止まるセクター＋減速時間〕で流す**（円盤が止まる前に配れるのでオンラインのラグが出ない） | [Assets/Scripts/Main/Roulette/RouletteMath.cs](Assets/Scripts/Main/Roulette/RouletteMath.cs) / [RouletteModel.cs](Assets/Scripts/Main/Roulette/RouletteModel.cs) / [SpinDecision.cs](Assets/Scripts/Main/Roulette/SpinDecision.cs) |
| ルーレット UI（Painter2D で虹色円盤・区切り線・中心ハブ・各セクターのキャラコイン下地を描画。**セクター総数は参加者数×K で動的**（`_sectorCount` を注入後に `RouletteMath.SectorCount` で確定・K＝`_numbersPerCharacter` 既定3）。**各セクターには割り振った参加者のキャラアイコン**をコイン（ゴールド枠＋白座面）で表示し（`CpuCharacterPicker` を共有して盤面コマとキャラを一致させる）、そのセクターの数字（`StepsForSector`）はアイコンの子バッジとしてコイン下部に重ねる。UI 構築はセクター総数の確定（Construct）と UI 要素取得（OnEnable）が両方そろってから走らせる（`TryBuildWheel`・順不同ガード）。アイコンは円盤と一緒に周回しつつ逆回転で常に正立。コイン/アバターの寸法はセクター数から弦長ベースで自動計算し重なりを防ぐ。キャラ画像は Addressables ロード・未配置は色面プレースホルダ。長押し中は加速・離すと減速する角速度回転を `Update` で駆動。離した後は離した瞬間の速度に依らず一定時間（2.5〜3.5 秒・ランダム）かけて ease-out で減速して止めるため、すぐ離しても長押しから離しても止まり方の印象が揃う。針の反応（セクター境界を通過するたびに Roulet のティック SE を鳴らす）・当たりセクター強調・結果ポップなどの演出。**停止時は円盤中央に「進むキャラ名（上）＋出目の数字（下）」を中央寄せグループ〔`ResultBox`〕で重ねて表示**する（数字＝`ResultLabel`・キャラ名＝`ResultName`。回転開始で空にし、停止＝`Stopped` 時に `RouletteModel.Result`／`AdvancingPlayer` の現在値から反映するので、CPU の手番や前回と同じ出目でも確実に出る。**キャラ名は停止時のみ `display` で出し回転中は隠す**。**進む人が自分〔人間プレイヤー〕のときはキャラ名の下に改行で「（あなた）」を添える**＝`ResolveAdvancingCharacterName`）。円盤本体（中央の出目数字含む）は `RouletteState` に連動して**回しているときだけ表示**し（`Spinning` で表示・`Stopped` で `_hideAfterStopSeconds` 秒後に非表示・`Idle`＝手番リセットで即非表示）、隠している間は `visibility` で透明化して背後の盤面（Sorting Order Board:0 の下層）を見せる（スピンボタンは手番トリガーとして常に残す）。スピンボタンは**画面左下のズームピルの右隣**（left:80px / bottom:16px）に絶対配置で固定し、文字ではなく画像（`Image/Roulette`＝`Roulette.png`）を貼って**円形**（border-radius でくり抜き）に表示する（`RouletteIconLoader.LoadSpinButtonAsync`・画像クラスは定数 `SpinButtonImageClass`・未配置なら「長押しで回す」の文字ボタンにフォールバック）。画像表示のときは**押下判定も内接円**にし、円の外＝四隅の押下は `OnPointerDown` の TrickleDown で弾く（`IsOutsideCircularButton`）。スピンボタンは**押し続けている間は有効のまま**（離す＝`PointerUp` を受け取る必要があるため）だが、**指を離してスピンが確定した瞬間に無効化**して惰性回転中の再押下を防ぐ（`_spinReleased` フラグを `UpdateSpinEnabled` の有効条件に組み込み、次の手番開始＝`SetInteractable(true)` でクリア）。手番制御 `SetInteractable`／CPU の自動スピン `AutoSpinAsync`／**他プレイヤーが回し始めた合図で一緒に回り出す `BeginRemoteSpin()`**〔手番制御に依らず回し、停止セクターが届くまで回り続ける＝相手が押している間もこちらの画面が一緒に回る〕／**他プレイヤーの結果を同じ演出で再生する `PlaySpinToAsync(sector, stopSeconds)`**〔既に回っていればそのまま減速へ入り、`RouletteSpinPhysics.ReleaseTo` で指定セクターの中心ちょうどに止める＝オンラインで全員の出目と止まる瞬間を一致させる。合図を取りこぼしていた場合だけここで回し始める〕／非表示になるまで待つ `WaitForHideAsync` を公開し `GameFlowController` から駆動される。**押下を離した時点で止まるセクターが確定する**（`ReleaseHold`＝`PredictStopRotation` の予測角を `NearestRotationForSectorCenter` でセクター中心へ寄せて `ReleaseTo`・確定は `RouletteModel.DecideSpin`→`Decided` で流す。停止時に `ParticipantForSector`/`StepsForSector` で `RouletteModel.CompleteSpin(steps, player)` へ）。円盤の Painter2D 描画は `RouletteWheelRenderer`・加減速の回転物理は `RouletteSpinPhysics`・当たり強調などの演出は `RouletteEffects`・画像ロードは `RouletteIconLoader` に分担） | [Assets/Scripts/Main/Roulette/RoulettePresenter.cs](Assets/Scripts/Main/Roulette/RoulettePresenter.cs) / [RouletteWheelRenderer.cs](Assets/Scripts/Main/Roulette/RouletteWheelRenderer.cs) / [RouletteSpinPhysics.cs](Assets/Scripts/Main/Roulette/RouletteSpinPhysics.cs) / [RouletteEffects.cs](Assets/Scripts/Main/Roulette/RouletteEffects.cs) / [RouletteIconLoader.cs](Assets/Scripts/Main/Roulette/RouletteIconLoader.cs) |
| 盤面データ（ScriptableObject。方眼キャンバス上にマスを**経路順**に並べて保持＝盤面の形・経路。各マスはイベント（進む/戻る/ミニゲーム・お金アップ/ダウン・陣地・アイテム。お金・陣地・アイテムは着地で発動し、進む/戻るは着地でそのマス数ぶん続けて動く〔連鎖・下記 `BoardPresenter.AdvanceAsync`〕。ミニゲームマスはそのマスに設定されたミニゲーム〔`MiniGame`〕を遊んで勝てば所持金報酬〕と見た目（色）を持つ。`Amount` は数値パラメータ（進む/戻るマス数。お金マスは着地のたびにランダム額を出すため `Amount` 不使用＝`MoneyCellRule`・陣地・アイテムも未使用）。マスの画像は**全マップ共通**（`BoardEventArtCatalog.AddressFor`＝静的カタログ。イベント種別ごとに `Board/<イベント名>` 規約で解決し、**ミニゲームマスだけはそのマスに設定されたゲームのサムネイル**〔`MiniGameCatalog.ImageAddress`＝`Image/MiniGame/*`〕を使ってどのゲームのマスか見分けられるようにする）で、盤面ごとの画像設定は持たない（同一イベントのマスすべてに同じ画像を貼る／`CharacterCatalog`・`ItemCatalog` と同じ静的カタログ方式）。全マス共通で画像の上に重ねる**枠画像**（`FrameAddress`）と、盤面の背後に画面全体で敷く**背景画像**（`BackgroundAddress`）は盤面ごとに持つ（どちらも任意）。画像は Addressables アドレスで、画像を用意していないイベント（ミニゲーム等）は記号表示にフォールバック。スタート＝ゴール（経路 index 0）は固定アドレス `Board/Start`（`BoardEventArtCatalog.StartAddress`）を優先。陣地マスは占拠後、所有者の旗画像へ差し替わる。マップ選択に出す**表示名**（`DisplayName`・`SetDisplayName`）も持つ。`CreateRectangular` で従来の矩形リングをメモリ生成しフォールバックに使う） | [Assets/Scripts/Main/Board/BoardDefinition.cs](Assets/Scripts/Main/Board/BoardDefinition.cs) / [BoardCellDefinition.cs](Assets/Scripts/Main/Board/BoardCellDefinition.cs) / [BoardCellEvent.cs](Assets/Scripts/Main/Board/BoardCellEvent.cs) / [BoardEventArtCatalog.cs](Assets/Scripts/Main/Board/BoardEventArtCatalog.cs) |
| 盤面エディタ（`Window > Sugoroku > Board Editor`。方眼をクリックして経路順にマスを置き、選択マスのイベント・数値（進む/戻るマス数。お金マスの額は着地時ランダムなので「金額」欄は効かない）・色を編集。グリッドのマスは**イベント種別ごとの色で塗り分け**（カスタム色未設定時。色定義はランタイム共通の `BoardEventColors` に一元化＝マップ選択のサムネイルと同じ配色・`BoardEditorGridView.EventColor` はそれに委譲）、色→イベントの対応表（凡例）をグリッド下に表示してどのマスに何のイベントが設定されているか一目で分かるようにする。ツールバーで**マップ名**（マップ選択に表示）・盤面共通の**枠画像アドレス**・画面全体に敷く**背景画像アドレス**・方眼サイズを設定（マスのイベント画像はイベント種別ごとに全マップ共通＝`BoardEventArtCatalog` なので盤面ごとの設定は無い）。`BoardDefinition` アセットの新規作成／読込／保存。方眼描画は `BoardEditorGridView`・右パネルは `BoardCellInspector` に委譲） | [Assets/Scripts/Main/Editor/BoardEditorWindow.cs](Assets/Scripts/Main/Editor/BoardEditorWindow.cs) |
| 盤面ロジック（位置前進（`Advance`・スタートを越えるとループ）・矩形リング→グリッド座標の純粋関数。座標は `BoardDefinition` データが持つのが基本で、これは矩形フォールバック生成に使う） | [Assets/Scripts/Main/Board/BoardMath.cs](Assets/Scripts/Main/Board/BoardMath.cs) |
| 盤面状態（コマ位置を**プレイヤーごと**に保持・移動中・勝者 index／`IsFinished`。移動完了は `EndMove`、勝者確定は `SetWinner`＝陣地を必要数（総数÷プレイヤー数の端数切り上げ）占拠時に `BoardPresenter` から呼ぶ） | [Assets/Scripts/Main/Board/BoardModel.cs](Assets/Scripts/Main/Board/BoardModel.cs) |
| 陣地マスの占拠状態（陣地マスの盤面 index ごとに所有者を保持・-1=未占拠。`Claim(player, index)` で占拠＝上書きで奪える・`Owner(index)` 購読で色替え・`CountOwnedBy(player)` で占拠数・`Changed`〔`Observable<Unit>`〕で占拠状態の変化を通知（Initialize / Claim で発火・ネームプレートの占拠数表示の購読に使う）・`RequiredToWin`＝勝利に必要な占拠数（陣地マス総数÷プレイヤー数の端数切り上げ・プレイヤー数は `GameParticipants` を注入して保持）・`HasReachedGoal(player)` で勝利判定・`CellsNotOwnedBy(player)` で自分以外が持つ陣地マス一覧（陣地獲得アイテムの選択対象）。陣地 index 一覧は `BoardPresenter` が `Initialize` で渡す。陣地マス 0 個の盤面は勝者が出ない） | [Assets/Scripts/Main/Board/TerritoryModel.cs](Assets/Scripts/Main/Board/TerritoryModel.cs) |
| 所持金（プレイヤーごとの所持金を保持。初期 1000・マイナス（借金）可。`Money(player)` 購読・`Add(player, delta)` で増減。お金マス着地・ミニゲーム報酬・お金よこどりアイテム（`BoardPresenter`）から呼ぶ） | [Assets/Scripts/Main/Money/MoneyModel.cs](Assets/Scripts/Main/Money/MoneyModel.cs) |
| お金よこどりで奪う額の純粋ロジック（相手の所持金が正のとき `MinFraction`(20%)〜`MaxFraction`(50%) の割合をランダムに奪う。端数切り捨て・最低1・上限＝相手の所持金にクランプ。0 以下なら 0。乱数源は呼び出し側が渡す＝テストで seed 固定可。残高移動は `MoneyModel`・奪取額の抽選は `BoardPresenter.DecideMoneySteal`／消費と演出は `ApplyMoneyStealAsync`） | [Assets/Scripts/Main/Money/MoneyStealRule.cs](Assets/Scripts/Main/Money/MoneyStealRule.cs) |
| お金マス着地時の増減額の純粋ロジック（マスごとの固定値ではなく `n × Unit`〔`Unit`=100・n は `MinN`(1)〜`MaxN`(5) のランダム〕の絶対値を返す。増える/減るの符号付けは呼び出し側＝`CellEventResolver.TryGetMoneyDelta`〔MoneyUp は +・MoneyDown は −〕。乱数源は呼び出し側が渡す＝テストで seed 固定可・実際の残高移動は `MoneyModel`・着地演出の統括は `BoardPresenter.ApplyLandingEventAsync`。`MoneyStealRule` と同じ規約） | [Assets/Scripts/Main/Money/MoneyCellRule.cs](Assets/Scripts/Main/Money/MoneyCellRule.cs) |
| アイテム種別・カタログ（`ItemId` enum〔StealTerritory/StealMoney/MiniGame/InstantWin〕と静的な `ItemCatalog`＝`ItemId`→表示名＋効果説明文（`Description`・モーダル本文に表示）＋アイテム絵アドレス〔`Image/Item/*`〕＋**購入価格（`Price`）**〔アイテムショップで所持金から支払う・種類ごと固定・初期所持金 1000 基準に調整〕。`All`/`Find(id)`/`RandomItem(System.Random)`／**`RandomLineup(rng, min, max)`**〔ショップに並べるランダムな枚数〔min〜max・カタログ総数でクランプ〕の重複なしラインナップを Fisher-Yates で抽選・seed 固定でテスト可〕を公開。新アイテムは enum とカタログに1行足す（価格も付ける）。`CharacterCatalog` と同じ静的カタログ方式。効果の発動は使用側〔`BoardPresenter.HandleItemUse`〕が担う＝`StealTerritory`〔陣地獲得〕・`MiniGame`〔ミニゲーム〕・`StealMoney`〔お金よこどり・奪う額は `MoneyStealRule`〕・`InstantWin`〔勝利＝使用即 `BoardModel.SetWinner` で自分の勝ち確定〕はすべて実装済み） | [Assets/Scripts/Main/Item/ItemId.cs](Assets/Scripts/Main/Item/ItemId.cs) / [ItemCatalog.cs](Assets/Scripts/Main/Item/ItemCatalog.cs) |
| 取得アイテム（手札）（プレイヤーごとに取得アイテムの `List<ItemId>` を保持。`Add(player, item)` で1枚加え取得を `Gained`〔R3 の `Observable<ItemGain>`〕で通知・`Use(player, item)` で1枚消費し `Used`〔`Observable<ItemUse>`〕で通知〔未所持なら false で何もしない。`ItemModel` 自体は消費と通知だけで、効果の発動は使用側〔`BoardPresenter.HandleItemUse`〕が担う〕・`Items(player)` で手札取得。アイテム購入〔アイテムショップ〕・アイテム効果の確定〔`BoardPresenter`〕から呼ぶ。`MoneyModel` と同じ Scoped DI・参加者ごと） | [Assets/Scripts/Main/Item/ItemModel.cs](Assets/Scripts/Main/Item/ItemModel.cs) |
| アイテム詳細モーダル（手札のカードをクリックすると開く。アイテム絵・名前・効果説明〔`ItemDefinition.Description`〕と「使用する」「閉じる」を表示し、「使用する」は生成側から渡された効果ハンドラ〔`Action<ItemId> onUse`＝`BoardPresenter.HandleItemUse`〕を呼んで閉じる〔消費〔`ItemModel.Use`〕・効果発動・手札 UI の減算はハンドラ側と `Used` 購読側が担う。選択キャンセルで消費しない効果があるため消費タイミングを効果側へ委ねる〕・「閉じる」/暗幕クリックで閉じる。`BoardLandingPresentation` と同じく `BoardPresenter` が `new` する協調クラスで、アイテム絵はロード済みキャッシュから引く〔未ロードは絵なしで名前と説明のみ〕。開いている間だけ Board の `UIDocument.sortingOrder` を一時的に 100 へ持ち上げ〔閉じたら元へ戻す〕、回転中のルーレット〔Sorting=10〕より前面に表示する。UI は Board.uxml の `ItemModal`＋`.item-modal*` クラス） | [Assets/Scripts/Main/Item/ItemModalPresenter.cs](Assets/Scripts/Main/Item/ItemModalPresenter.cs) |
| アイテムショップモーダル（アイテム取得マス着地時に開く。`ItemCatalog.RandomLineup` で抽選したランダムなラインナップを「絵・名前・効果説明・価格」の商品カードで並べ、タップで1つ購入〔選んだ `ItemId` を `SelectAsync` で返す。代金の支払い・手札追加は呼び出し側＝`BoardPresenter` が担う〕。**一度に `CardsPerPage`＝2 枚だけビューポートに見せ、残りは前後ボタン〔‹ ›〕＋ドットのカルーセルでページ送り**する〔`ShowPage` がトラックを `PageWidthPx`＝カード142px×2 ぶん translate。カードは `flex-shrink:0`＝縮まずはみ出しを `overflow:hidden` で切る。2枚以下ならページ送り UI は隠す〕。所持金〔`budget`〕で買えないカードは無効表示〔`item-shop-card--disabled`・価格は赤字〕。**誤タップ防止で暗幕クリックでは閉じない**〔「買わずに閉じる」ボタン・破棄で null＝買わない〕。`MiniGameSelectPresenter` と同じく `BoardPresenter` が `new` する協調クラスで、開いている間だけ Board の `UIDocument.sortingOrder` を 100 へ持ち上げる。アイテム絵は `BoardPresenter.LoadItemSpriteAsync` 経由でロードさせ `_itemSprites` にキャッシュ〔購入後の手札サムネイルも同じキャッシュから引ける〕。UI は Board.uxml の `ItemShopModal`＋`.item-shop*`／`.item-shop-card*` クラス） | [Assets/Scripts/Main/Item/ItemShopPresenter.cs](Assets/Scripts/Main/Item/ItemShopPresenter.cs) |
| ミニゲーム選択モーダル（ミニゲームアイテム使用時に「どのミニゲームを遊ぶか」を選ばせる。`MiniGameCatalog` の各ゲームを「サムネイル画像（`MiniGameDefinition.ImageAddress`＝`Image/MiniGame/*` を Addressables ロード・未配置は名前のみ）＋ゲーム名」のカードで横並びに並べ〔`MiniGameSelectList`〕、選んだ ID を `SelectAsync` で返す〔キャンセル・暗幕クリック・破棄では null＝消費しない〕。`ItemModalPresenter` と同じく `BoardPresenter` が `new` する協調クラスで、開いている間だけ Board の `UIDocument.sortingOrder` を 100 へ持ち上げてスピンボタン等より前面に出す。UI は Board.uxml の `MiniGameSelectModal`＋`.minigame-card*`／`.minigame-select__list` クラス〔暗幕・カード枠は `.item-modal*` を流用〕） | [Assets/Scripts/Main/Item/MiniGameSelectPresenter.cs](Assets/Scripts/Main/Item/MiniGameSelectPresenter.cs) |
| 盤面 UI（描画する `BoardDefinition` は `ResolveDefinition` が「(1) MapSelect で選ばれたマップ〔`_catalog.Find(BoardSessionModel.SelectedId)`〕→ (2) インスペクタ割り当ての `_definition` → (3) `_columns`/`_rows` から矩形リング生成」の順で解決。選択マップは注入後に参照するため `BuildCells` は Construct と OnEnable の両方がそろってから走るようゲートする。既定は縦長リング 5列×7行＝周回20マス。リング領域はグリッドの `(列-1):(行-1)` のアスペクト比を保ってピクセルで中央配置（`LayoutBoardArea`・`GeometryChangedEvent` でリサイズ追従）し、画面比に依らずマスを均等に並べる。マス中心間隔はマスの実寸（端の最外周マスは領域端から半マスはみ出す）まで含めて利用可能領域に収まるよう決め、最外周マスが余白へ食い込まず画面端に隙間を残す。各マスの一辺はマス中心間隔の `_cellFillRatio` 倍にして、マス間に隙間を作る（`ResizeCells`）。隙間はマス中心を経路順に結ぶ接続線でつなぐ（`board-lines` オーバーレイに `generateVisualContent`＋Painter2D で描画・最後のマスからスタートへ戻ってループを閉じる）。盤面の背後に画面全体で敷く**背景画像**（`BoardDefinition.BackgroundAddress`）を Addressables ロードして最背面の `BoardBackground`（`.board-background`）へ貼る＝`StartLoadingBackgroundIfReady`/`LoadBackgroundAsync`〔未設定・未配置なら背景なし・ズーム/パンには追従しない画面全面固定・`-unity-background-image-tint-color` で少し暗くして盤面を見やすくする〕。マス描画（データの色・マス画像＝全マップ共通の `BoardEventArtCatalog.AddressFor` で解決して Addressables ロード〔イベント種別ごと・ミニゲームマスはゲーム別サムネイル〕・その上に盤面共通の枠画像を各マスへオーバーレイ＝`AddFrameOverlay`/`LoadFrameAsync`・イベント記号 ▲進む/▼戻る/MG/陣/カ を反映・陣地マスは占拠者の**旗画像**に塗り替え＝`SetupTerritoriesIfReady` で `TerritoryModel.Owner` を購読し `ApplyTerritoryOwner` が占拠者の旗画像〔`_flagIcons`〕を貼る〔`board-cell--flag`＝scale-to-fit・所有者色は枠線で残す〕・旗未ロード時は色クラスのみ）・参加者ぶんのコマ描画（キャラの丸バッジ画像＝`PieceIconAddress` を Addressables ロードして貼付、YOU＝選択キャラ・CPU＝人間とも他 CPU とも別のキャラを配布〔`CpuCharacterPicker`〕。コマ色はプレイヤーごとに p0〜p7 の 8 色〔`PlayerColors`〕・画像未配置のキャラは色＋YOU/CPU ラベルにフォールバック。**同じマスに複数コマが乗ったら円状にずらして全員見えるようにする**＝`RefreshPieceOffsets`〔占有状況から毎回組み直し・単独は中央〕）・画面上部に**全プレイヤー**のネームプレート（`PlayerNameplateView`＝横 1 行に並べ、1 画面最大 2 人・超えるぶんは左右端の三角ボタンでページ送り〔4 人＝2 ページ〕。各プレートは横型で左にキャラの丸アイコン〔`PieceIconAddress`〕、右に選択キャラ名・所持金・占領地数を縦に並べ、上辺アクセントをそのプレイヤー色〔`board-nameplate--p{N}`〕にして盤面の色分けの凡例にする。所持金は `MoneyModel` を購読しコインアイコン＋金額でリアルタイム更新〔マイナスは赤字〕、占領地は `TerritoryModel.Changed` を購読し「占拠数 / 勝利に必要な数〔`RequiredToWin`＝総数÷プレイヤー数の切り上げ〕」で表示〔陣地マス 0 個の盤面では非表示〕。所持金・占領地の行頭アイコンは `Image/Icon` の `CoinIcon`／`TeritoryIcon` を Addressables ロード〔未配置は USS 描画バッジにフォールバック〕・右上のオプションアイコンを避けて中央寄せ＝`BuildPlayerHeaderIfReady`）・コマ移動演出。ルーレット出目とミニゲームのボーナスを共用する `AdvanceAsync(player, steps)`（**移動開始時にズームを既定へ戻して動くコマを画面中央に据え**〔`FocusCameraOnPlayer`→`BoardZoomController.CenterOn`〕、周回で止めず出目ぶん進み、**1マス進むごとに少し間〔`_panFollowDelay`・既定0.09秒〕を置いてカメラをそのマスへパン追従させる**。移動完了後に `PlayLandingSequenceAsync` で着地演出を再生。**止まったマスが進む/戻るなら、そのマス数ぶん続けて動く**〔`MoveAndLandAsync` を繰り返す連鎖・`TryGetChainedSteps`＝`CellEventResolver.TryGetMoveSteps` で進む＝+`Amount`／戻る＝-`Amount`。戻るはスタートを跨ぐと盤面の末尾へ回り込む〔`BoardMath.Advance` の剰余〕・演出は通常の移動と同じ・連鎖先の着地イベントも通常どおり発動する・上限は `MaxChainedMoves`＝8 回で無限ループを断つ。マス数は盤面データなので全クライアントで一致＝オンラインでも配らない〕）・着地演出（`PlayLandingSequenceAsync`＝止まったマスの種別で分岐：陣地マスは旗演出→占拠確定（すでに自分が占拠しているマスは旗演出をスキップ）・ミニゲームマスはそのマスに設定されたミニゲームを起動して勝てば所持金報酬〔`PlayMiniGameCellSequenceAsync`＝遊ぶのは着地した本人だけで結果の報酬額を `MiniGameLanding` で配る・CPU は遊ばせるものが無いので決める人が勝敗を抽選・種類はマスごとに固定なので選択モーダルは出ない〕・アイテムマスは**アイテムショップ**〔`ItemShopPresenter`・ランダムなラインナップを所持金で購入・一度に2枚のカルーセル・CPU は自動購入〕を開き、購入したら代金を `MoneyModel` から支払い〔`MoneySE`〕買ったアイテムを右下の手札〔`.item-hand`〕へ追加＝同じアイテムはカードを増やさず既存カードの枚数バッジ〔`.item-hand__count`〕を「x2」のように更新（`AppendItemToHand`）・手札カードはクリックでアイテム詳細モーダル〔`ItemModalPresenter`〕を開き、使用（`ItemModel.Used` 購読）で枚数バッジを減算＝最後の1枚ならカード削除（`RemoveItemFromHand`）・お金マスは増減額の浮遊テキスト・進む/戻るマスは動くマス数〔+n マス / -n マス〕の浮遊テキスト〔`ShowMoveFloatAsync`＝お金と共通の演出〕を出してから連鎖の移動へ入る・それ以外はマス画像の中央ポップ。演出フロー・秒数の詳細は [docs/architecture.md](docs/architecture.md)「シーン構成」の着地演出の項に集約）・アイテム効果の統括（`HandleItemUse`＝**効果のパラメータを決めて `OnlineGameSync` へ発行するだけ**で、消費〔`ItemModel.Use`〕と効果の適用は受信側の `ApplyActionAsync` が担う〔決定と適用を分けることでオンラインでも全員が同じ効果を同じ順に反映する。キャンセルされた使用は発行されないのでそもそも消費されない〕。**着地の乱数・モーダルも同じ規約**＝お金マスの増減額とアイテムショップの品揃え/購入は着地した本人だけが決めて発行し〔`MoneyLanding`／`ShopResult`〕、代金の支払い・手札への追加・所持金の増減は全員が受信して適用する。アイテム種別で分岐。陣地獲得〔`StealTerritory`〕は `TerritoryModel.CellsNotOwnedBy` で対象マスを出し、金枠〔`board-cell--selectable`〕＋キラキラのリング〔`board-cell__glow` を `AnimateSelectableGlowAsync` で ping パルス駆動〕で強調・ガイドバナー〔`TerritorySelectBanner`〕を出し、盤面タップ〔`BoardZoomController.BeginCellSelection` 経由でタップ/パン両立〕またはキャンセルを待って占拠→旗演出→必要数なら勝利。ミニゲーム〔`MiniGame`〕は `MiniGameSelectPresenter` で遊ぶゲームを選ばせ〔キャンセルで消費せず終了〕、`MiniGameLauncher` で起動して勝てば `MoneyModel.Add` で所持金報酬〔勝敗は `DetermineMiniGameWin`＝各ゲームがスコア1で勝ちを報告する共通判定〔`Score==1`〕・レースは先着・タップ連打は連打数1位〔CPU もゲーム内で自動連打〕・被っちゃやーよは獲得〕。選択・演出中は `RoulettePresenter.SetInteractable(false)` でスピン無効・確定時のみ `ItemModel.Use` で消費・効果はターン非消費。勝利〔`InstantWin`〕は使用即 `ItemModel.Use` で消費し `BoardModel.SetWinner(_humanPlayer)` を呼ぶだけの最小の即時効果＝勝者表示・花火は既存の `Winner` 購読が担う）・手番アナウンス（`TurnModel.CurrentPlayer` を購読し、手番が移るたびに「〔キャラ名〕の番」（自分＝人間プレイヤーの手番は「あなたの番」）のアナウンス帯〔`TurnBanner`／`.turn-banner`〕を画面中央上寄りに約1.4秒フェード表示＝`ShowTurnBanner`。購読は `SetupBannersIfReady` で1度だけ張り購読時発火で初手番も出す・キャラ名は `CpuCharacterPicker.ResolveCharacter` で解決・入場SEを添える）・**相手の操作待ちの表示**（`WaitingBanner`／`.waiting-banner`＝盤面上部に「〔キャラ名〕が◯◯中…」を出す。`SetBusy`〔`Busy` 受信・アイテムショップ待ち〕と `RefreshWaitingBanner`〔手番・ルーレット状態の変化〕で更新し、文言は `ResolveWaitingMessage` が「(1) 相手が操作中〔買い物／ミニゲーム／陣地選択〕→ (2) 相手の手番でルーレット未回転＝ルーレット待ち → (3) 出さない」の優先度で決める。末尾の「.」は固定幅の別ラベル〔`WaitingBannerDots`〕で動かしピルの幅を一定に保つ。コマ移動中・着地演出中・アイテム効果中・決着後・退出後・一人用モードでは出ない）・勝敗メッセージ表示（勝者確定〔`BoardModel.Winner`〕で勝者のキャラ名を使って「〔キャラ名〕の勝ち！」を表示〔`WinnerText`＝`CpuCharacterPicker.ResolveCharacter` でキャラを解決〕し、「ホームに戻る」ボタン〔`GameOverActions`／`HomeReturnButton`〕を盤面下部に出し、押すと `ReturnHomeAsync` が**オンラインセッションを離脱してから**〔NGO の停止も UGS 側が行う・離脱しないとルームに残る〕 `SceneTransitioner.Transit(Scenes.Home)` で Home へ戻る＝`_returningHome` で連打・多重遷移をガード。**決着時は `ScreenEffectPlayer` でパーティクルを画面前面に再生＝勝者が人間プレイヤーなら花火・敗北〔CPU の勝利〕なら雨**〔インスペクタ割り当ての `_victoryEffectPrefab`／`_defeatEffectPrefab`と共通の `_victoryEffectShader`〔`Sugoroku/AdditiveUI`〕・距離/縦オフセット/スケール/発数/間隔を SerializeField で調整。勝利用・敗北用で別インスタンス・Prefab 未設定ならその側は再生しない〕）。ズーム/パンは `BoardZoomController`、着地演出のビュー（ポップアップ・浮遊テキスト・旗トゥイーン）は `BoardLandingPresentation` に委譲・リング領域のレイアウトとズーム用の寸法（`Area`）は `BoardLayoutCalculator`） | [Assets/Scripts/Main/Board/BoardPresenter.cs](Assets/Scripts/Main/Board/BoardPresenter.cs) / [BoardLandingPresentation.cs](Assets/Scripts/Main/Board/BoardLandingPresentation.cs) / [BoardLayoutCalculator.cs](Assets/Scripts/Main/Board/BoardLayoutCalculator.cs) / [PlayerNameplateView.cs](Assets/Scripts/Main/Board/PlayerNameplateView.cs) |
| 盤面のズーム・ドラッグパン（横長マップ向け。虫眼鏡 +／− ボタンで「画面幅に収める列数」を段階切替＝`_zoomColumnLevels`〔既定 `{2,3,4,6,8}`〕を行き来し、各段階のスケールを基準列数レイアウトに掛ける〔`ScaleForColumns`〕。段階は盤面の実列数を上限に丸める〔`BuildColumnLevels`〕。既定は `_visibleColumns`＝4 列で `BoardLayoutCalculator` が盤面を横へはみ出させ、はみ出したぶんを全画面の透明ドラッグ層〔`BoardDragLayer`・盤面がはみ出しているときだけ picking 有効〕でドラッグしてパン〔`ClampPan` で盤面が画面を覆う範囲にクランプ〕。ズームは**カメラ（画面）中心**で、段階切替の直前に画面中央に見えている盤面上の点が動かないようパンを補正してから拡大縮小する〔`ZoomAroundViewportCenter`→`ClampPan`。右上を見ている状態でズームすればそのまま右上が拡大される〕。起動時はスタート＝左上へ寄せる。**コマ移動中はカメラがコマに追従する**＝`BoardPresenter` が正規化位置を渡して呼ぶ `CenterOn`（対象マスが画面中央に来るようパンを解き `ClampPan` で盤面が画面を覆う範囲へ収める・端付近では寄り切らない・移動開始時は先にズームを基準列数へ戻す）。ボタン UI は **+ と − を1つの縦長カプセルに連結した「ズームピル」**（金の極細枠＋暗いガラス地・`.zoom-pill`）で、記号は金色・間を区切り線で分ける。虫眼鏡画像 `Image/lupe` は Addressables ロードできたときだけ**ピル中央の飾りアイコン**として表示する〔`LoadMagnifierIconAsync`・未配置なら + と − が直接隣り合う〕。**陣地獲得アイテムのマス選択モード**〔`BeginCellSelection(Func<Vector2,bool>)`／`EndCellSelection`〕も持ち、選択中はドラッグ層を常時反応させて、押下からほぼ動かず離せば**タップ＝選択**・動かせば**ドラッグ＝パン**に振り分ける〔盤面タップとパンを両立・`BoardPresenter` から駆動〕） | [Assets/Scripts/Main/Board/BoardZoomController.cs](Assets/Scripts/Main/Board/BoardZoomController.cs) |
| 決着エフェクト（任意のパーティクル Prefab を画面前面で再生する汎用協調クラス。人間プレイヤーの勝利で花火〔既定は CFXR の花火〕、敗北〔CPU の勝利〕で雨〔既定は CFXR4 Rain Falling〕を出す。`BoardPresenter` が勝利用・敗北用の別インスタンスを持ち、SerializeField の Prefab・共通シェーダー・距離/縦オフセット/スケール/発数/間隔を渡して所持。UI Toolkit の ScreenSpaceOverlay はワールド空間のパーティクルを覆い隠すため、専用の `Effect` レイヤー〔TagManager index 6〕だけを映すエフェクトカメラ→`RenderTexture`→加算ブレンド Canvas〔`sortingOrder` 200〕の `RawImage` で前面合成する〔`Sugoroku/AdditiveUI` シェーダー＝`Blend One One` で黒背景は透明・光だけ乗る〕。発数ぶん横に広げ間隔を空けて打ち上げ、生成物〔カメラ・Canvas・RT〕を後片付け〔`keepUntilCancelled` で挙動を切替：花火は実再生時間ぶん待って破棄、雨はシーンを出る〔ct キャンセル〕まで降らせ続けてから破棄〕。詳細は [docs/effects.md](docs/effects.md)） | [Assets/Scripts/Main/Board/ScreenEffectPlayer.cs](Assets/Scripts/Main/Board/ScreenEffectPlayer.cs) / [Assets/Shaders/AdditiveUI.shader](Assets/Shaders/AdditiveUI.shader) |
| ミニゲーム起動（Main を残して MiniGame シーンを Additive で重ね・終了後に単独アンロード。Transit は使わない。MiniGameTest シーンとミニゲームアイテムの効果〔`BoardPresenter.DecideMiniGameAsync`〕から起動される） | [Assets/Scripts/Common/MiniGame/MiniGameLauncher.cs](Assets/Scripts/Common/MiniGame/MiniGameLauncher.cs) |
| ミニゲーム種別・カタログ（種別→表示名・UXML アドレス・選択カードのサムネイル画像アドレス〔`Image/MiniGame/*`〕。新規追加はここに1行）・結果・起動側↔ホストの仲介 | [Assets/Scripts/Common/MiniGame/MiniGameId.cs](Assets/Scripts/Common/MiniGame/MiniGameId.cs) / [MiniGameCatalog.cs](Assets/Scripts/Common/MiniGame/MiniGameCatalog.cs) / [MiniGameResult.cs](Assets/Scripts/Common/MiniGame/MiniGameResult.cs) / [MiniGameSessionModel.cs](Assets/Scripts/Common/MiniGame/MiniGameSessionModel.cs) |
| ミニゲームホスト（`CurrentGame` でゲームごとに分岐するディスパッチャ。UXML を Addressables ロードして、タップ連打は `TapGamePlay`、2Dレースは `RaceGamePlay`、被っちゃやーよは `OverlapGamePlay` へ委譲する） | [Assets/Scripts/MiniGame/MiniGameHostPresenter.cs](Assets/Scripts/MiniGame/MiniGameHostPresenter.cs) |
| タップ連打ロジック（フェーズ・各参加者の連打数・残り時間の純粋ロジック。**参加者はプレイヤー〔index 0〕＋CPU〔1〜〕で `Setup(playerCount, seed)` で人数を受け、各 CPU は `Tick` で自動連打する**〔速度は `TapGameConfig`＝回/秒レンジ・`System.Random` で決定的〕。`ParticipantCount`／`TapCountOf(int)`／`IsPlayerWin`〔連打数1位＝勝ち・同数はプレイヤー勝ち〕を公開。プレイヤーの連打数 `TapCount` は reactive で互換維持） | [Assets/Scripts/MiniGame/TapGame/TapGameModel.cs](Assets/Scripts/MiniGame/TapGame/TapGameModel.cs) / [TapGameConfig.cs](Assets/Scripts/MiniGame/TapGame/TapGameConfig.cs) |
| タップ連打 UI・進行（ホストから委譲され、選択中キャラのカード絵（`CardAddress`）をタップボタンの上に表示し、タップのたびにカードだけを「がたがた」振動＋「パンチ」拡大で弾ませる（ボタン本体は固定）。タップのたびにパンチ SE（`SoundStore.RandomPunchSE`＝Punch1〜3 をランダム）を鳴らす。タップ数はボタン上に重ねて表示し、カウントダウン中（計測開始前）は非表示。残り時間は「残り時間：」付きで表示。**画面上部のスコアボードに全参加者のキャラアイコン〔`PieceIconAddress`〕＋連打数を横一列で並べ、進行中は毎フレーム `Tick`＋更新して他プレイヤーの連打数をライブで見せる**〔自分のチップは金枠で強調・多人数は折り返し〕。終了時は自分の順位を表示。カード・アイコン画像は Addressables ロード・未配置は色面プレースホルダ） | [Assets/Scripts/MiniGame/TapGame/TapGamePlay.cs](Assets/Scripts/MiniGame/TapGame/TapGamePlay.cs) |
| 2Dレース ロジック（進捗0→1・メーター判定（`Judge`）・タップ加算（`ApplyTap`）・時間経過（`Tick`）・勝敗の純粋ロジック。**走者はプレイヤー〔index 0〕＋CPU〔1〜〕で、`Setup(config, playerCount, seed)` で人数を受ける**〔`RunnerCount`／`Progress(int)`／`WinnerIndex` を公開・`Setup(config, seed)` は 2 走者の互換版〕。各 CPU はプレイヤーと同じベース速度で進み、**独立したタイマー**でランダムに Great/Good/Miss を抽選して前進（Great は低確率）。決着は先着＝進捗最大の走者・同値は若い index〔プレイヤー優先〕。`System.Random` で決定的。速度・ブースト量・判定帯幅・CPU 抽選確率は `RaceGameConfig` に定数化〔全 CPU 共通〕） | [Assets/Scripts/MiniGame/RaceGame/RaceGameModel.cs](Assets/Scripts/MiniGame/RaceGame/RaceGameModel.cs) / [RaceGameConfig.cs](Assets/Scripts/MiniGame/RaceGame/RaceGameConfig.cs) / [RaceGamePhase.cs](Assets/Scripts/MiniGame/RaceGame/RaceGamePhase.cs) / [MeterJudgement.cs](Assets/Scripts/MiniGame/RaceGame/MeterJudgement.cs) |
| 2Dレース UI・進行（ホストから委譲され、走者スプライト（`RunAddress`）・往復メーター・カウントダウン・判定表示・結果を毎フレーム駆動。メーターのアニメと入力は Presenter、判定/前進/勝敗は Model。走者は右→左へ進み先着で勝ち。スコアは勝ち=1／負け=0。画像未配置は色面プレースホルダ。**参加者数〔`MiniGameSessionModel.PlayerCount`〕に応じて Track にレーンを動的生成し、走者サイズ・レーン高さを人数で自動縮小する**。各走者のキャラは `MiniGameSessionModel.Characters`〔起動側が渡す・本番は実参加者のキャラ、MiniGameTest はランダム〕で解決し、レーンのタグは**キャラ名**〔YOU/CPU でなく・プレイヤー/CPU の色分けクラスは残す〕。セッション未指定時は従来解決＝プレイヤーは選択キャラ・CPU は `RaceOpponentPicker.PickMany` の重複なしランダム配布にフォールバック） | [Assets/Scripts/MiniGame/RaceGame/RaceGamePlay.cs](Assets/Scripts/MiniGame/RaceGame/RaceGamePlay.cs) / [RaceOpponentPicker.cs](Assets/Scripts/MiniGame/RaceGame/RaceOpponentPicker.cs) |
| 被っちゃやーよ ロジック（参加者数ぶんの重複なしアイテムを `ItemCatalog` から抽選して提示し、各参加者が1つ選ぶ。プレイヤー＝index 0 の選択が他の誰とも被らなければ獲得＝勝ち。CPU の選択は `Setup(playerCount, seed)` 時点で `System.Random` により確定＝決定的。提示枚数は参加者数（`MiniGameSessionModel.PlayerCount`＝本番の盤面ミニゲームは2固定・MiniGameTest はステッパーで2〜4。未設定時のフォールバックが `OverlapGameConfig.DefaultPlayerCount`＝2）とカタログ総数の小さい方にクランプ。フェーズ Ready/Countdown/Choosing/Revealed/Finished。制限時間内に選べなければ `TimeOut()` で無効票＝未選択のまま Revealed へ進み `IsPlayerWin` は false。時間・入力は Presenter 駆動でここは純粋ロジック） | [Assets/Scripts/MiniGame/OverlapGame/OverlapGameModel.cs](Assets/Scripts/MiniGame/OverlapGame/OverlapGameModel.cs) / [OverlapGameConfig.cs](Assets/Scripts/MiniGame/OverlapGame/OverlapGameConfig.cs) / [OverlapGamePhase.cs](Assets/Scripts/MiniGame/OverlapGame/OverlapGamePhase.cs) |
| 被っちゃやーよ UI・進行（ホストから委譲され、提示アイテム数（＝参加者数）に応じて選択カードを動的生成し、アイテム絵〔`Image/Item/*`〕を並列ロードして貼る。カウントダウン → 選択（`OverlapGameConfig.ChoiceSeconds`＝3秒の制限時間を「のこりN」で表示・時間切れは `TimeOut`）→ 全員オープン（**カードを選んだ参加者ぶんのキャラ名バッジを動的生成**〔`MiniGameSessionModel.Characters`・プレイヤー＝青/CPU＝赤・複数人が同じカードなら並べて wrap・未指定時は YOU/CPU〕・被り＝赤枠／セーフ＝緑枠）→ 結果（獲得＝ItemGetSE・かぶり/時間切れ＝Cancel1SE）を駆動。スコアは獲得=1／被り・無効票=0。画像未配置は色面プレースホルダ） | [Assets/Scripts/MiniGame/OverlapGame/OverlapGamePlay.cs](Assets/Scripts/MiniGame/OverlapGame/OverlapGamePlay.cs) |
| ミニゲーム動作確認シーン（`MiniGameCatalog` の各ミニゲームをボタンで一覧し、**−／＋ の人数ステッパー〔2〜4・`PlayerCountSessionModel.Min`/`Max`〕で参加者数を選んで**押すと `MiniGameLauncher` で起動→結果スコアを表示〔選んだ人数を `PlayAsync` の `playerCount` 引数で渡す＝被っちゃやーよのカード数・2Dレースのレーン数・タップ連打の CPU 数（スコアボード）に反映。本番の盤面ミニゲームは2人固定〕。**各参加者にはランダムな重複なしキャラを割り当てて `characters` 引数で渡す**〔`RandomCharacters`＝CharacterSelect を通らないテストシーンでも YOU/CPU でなくキャラ表示にする〕。本番フローには出さず、エディタで `MiniGameTest` シーンを直接開いて Play する） | [Assets/Scripts/MiniGame/Test/MiniGameTestPresenter.cs](Assets/Scripts/MiniGame/Test/MiniGameTestPresenter.cs) / [MiniGameTestLifetimeScope.cs](Assets/Scripts/MiniGame/Test/MiniGameTestLifetimeScope.cs) |
| タイトル背景動画＋タイトル文言演出（StreamingAssets の動画を `VideoPlayer`→`RenderTexture` で全画面背景に再生し、終了後に「ドラゴンファミリー/すごろく」を3行・1文字ずつ上から降らせる。初回再生開始から30秒おきに文言を隠して最初から再生し直すループ。初回起動時は動画の再生準備が完了するまで画面右下に「Now Loading」を表示し、準備の成否が確定した時点で隠す。直接起動でも初回再生されるよう `Start` と `ReadyAsync` の両方で初期化。準備タイムアウト・再生エラー時は文言のみ表示） | [Assets/Scripts/Title/Video/Presenter/TitleVideoPresenter.cs](Assets/Scripts/Title/Video/Presenter/TitleVideoPresenter.cs) |
| タイトル動画ファイル（StreamingAssets。H.264 baseline / BT.709 タグ付き mp4） | [Assets/StreamingAssets/Video/TitleMovie.mp4](Assets/StreamingAssets/Video/TitleMovie.mp4) |
| 日本語フォント（アセット） | [Assets/Font/](Assets/Font/) |
| 既定フォント設定（全 UI へ NotoSansJP Bold を適用） | [Assets/UI Toolkit/UnityThemes/UnityDefaultRuntimeTheme.tss](Assets/UI%20Toolkit/UnityThemes/UnityDefaultRuntimeTheme.tss) |

## ドキュメント

- [docs/architecture.md](docs/architecture.md): アーキテクチャドキュメント
- [docs/design-system.md](docs/design-system.md): UIデザインシステム（カラー・タイポグラフィ・コンポーネント）
- [docs/patterns.md](docs/patterns.md): よく触る実装パターン集（Presenter追加・DI登録・destroyCancellationToken・DOTween×UI Toolkit・DOTween×R3 の AddTo 衝突・RegisterComponentInHierarchy の前提・Button の押し続け判定はトリクルダウン登録・オンライン同期は決定と適用を分ける・新しいミニゲーム/アイテム/マスイベントを追加する手順）
- [docs/effects.md](docs/effects.md): パーティクル・VFX 実装ノウハウ（UI Toolkit との共存・加算ブレンド・worldBound 変換・再生時間調整）
- [docs/product.md](docs/product.md): プロダクトドキュメント
- [docs/matchmaking.md](docs/matchmaking.md): マッチメイキング設計（UGS Multiplayer Services）
- [docs/Live2D.md](docs/Live2D.md): Live2D Cubism SDK のアニメーション実装ノウハウ（**Live2D 関連の実装前に必読**）
- [docs/networking.md](docs/networking.md): NGO + MPM ネットワーク実装ノウハウ（**NGO 関連の実装前に必読**）

## Asset Store アセット

- Asset Store からダウンロードしたものは `Assets/AssetStore/` に配置する。このディレクトリは Git の管理対象外。
- DOTween (Demigiant) は `Assets/Plugins/` に配置済み（Git 管理対象）。
- Live2D Cubism SDK は `Assets/Live2D/` に配置済み（Git 管理対象）。
  - `Assets/csc.rsp` / `Assets/mcs.rsp` に `-unsafe` フラグが必要（Cubism Core が unsafe コードを使用するため）。
